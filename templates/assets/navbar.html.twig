<nav class="navbar navbar-expand-lg navbar-light">
  <a class="navbar-brand" href="{{ path('home') }}"><i class="fas fa-car"> CarDealer</i></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
       {% if app.user %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('create') }}">Create New AD</a>
      </li>
      {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('models_new') }}">New Vehicle</a>
      </li>   
      {% endif %}
       
      {% endif %}
      {% if app.user %}
      <li class="nav-item dropdown  float-right">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          {{app.user.username}}
        </a>

            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="{{ path('password_reset') }}">Change password</a>
              <a class="dropdown-item" href="{{ path('edituser') }}">Change contact data</a>
               <a class="dropdown-item" href="{{ path('search', {'contact':app.user.id}) }}">My ads</a>
                <a class="dropdown-item" href="{{ path('carsfavorites') }}">My favourite ads</a>
              <div class="dropdown-divider"></div>
              <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
            </div>  
                 
      </li>
       {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login') }}">Login</a>
        </li>
                <li class="nav-item">
          <a class="nav-link" href="{{ path('register') }}">Register</a>
        </li>
      {% endif %}

    </ul>

   
    <ul class="navbar-nav float-right">
      <li id="messages" class="px-2"></li>
      <li class="nav-item "><a href="{{ path('message') }}"><i class="far fa-envelope"></i></a></li> 
    </ul>
  </div>
</nav>

  <div class="row py-1 bigicons">
   <div class="col">
    <a class="px-2" style="  {% if app.request.pathinfo starts with '/cars' %} {{'color:rgba(241,164,26,1)'}}{% endif %}" href="{{ path('ads_index') }}"><i class="fas fa-car fa-2x"> Cars</a></i>
    <a class="px-2" style="  {% if app.request.pathinfo starts with '/bikes' %} {{'color:rgba(241,164,26,1)'}}{% endif %}"   href="{{ path('bikes_index')}}"><i class="fas fa-motorcycle fa-2x"> Bikes</a></i>
    <a class="px-2"  href="#"><i class="fas fa-truck fa-2x"> Trucks</i></a>
   </div>
  </div>




